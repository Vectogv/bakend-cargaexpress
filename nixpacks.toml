[phases.setup]
nixPkgs = ["nodejs", "nodePackages.pnpm"]
# Cliente MySQL (mysqldump) para app/services/backup_service.ts
aptPkgs = ["default-mysql-client"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "bash start.sh"
